import { Check, Circle } from 'lucide-react'
import { Link } from 'react-router-dom'

export default function SetupChecklist({title,steps}){const complete=steps.filter(step=>step.done).length,percent=Math.round(complete/steps.length*100);return <section className="card p-5"><div className="flex items-center justify-between"><div><h2 className="font-display font-bold text-fg">{title}</h2><p className="text-xs text-fg-muted">{complete} of {steps.length} completed</p></div><strong className="text-teal-700 dark:text-teal-300">{percent}%</strong></div><div className="mt-3 h-2 rounded-full bg-surface-inset"><div className="h-2 rounded-full bg-teal-600" style={{width:`${percent}%`}}/></div><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-5">{steps.map(step=><Link key={step.label} to={step.to} className={`flex min-h-12 items-center gap-2 rounded-xl border p-3 text-xs font-bold ${step.done?'border-teal-100 bg-teal-50 text-teal-800 dark:border-teal-800 dark:bg-teal-500/10 dark:text-teal-300':'border-line bg-surface-inset text-fg-muted'}`}>{step.done?<Check size={17}/>:<Circle size={17}/>} {step.label}</Link>)}</div></section>}
