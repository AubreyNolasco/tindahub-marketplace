import { Check, Circle } from 'lucide-react'
import { Link } from 'react-router-dom'

export default function SetupChecklist({title,steps}){const complete=steps.filter(step=>step.done).length,percent=Math.round(complete/steps.length*100);return <section className="mb-6 rounded-2xl border border-black/[.06] bg-white p-5 shadow-card"><div className="flex items-center justify-between"><div><h2 className="font-display font-bold">{title}</h2><p className="text-xs text-ink/45">{complete} of {steps.length} completed</p></div><strong className="text-teal-700">{percent}%</strong></div><div className="mt-3 h-2 rounded-full bg-black/[.06]"><div className="h-2 rounded-full bg-teal-600" style={{width:`${percent}%`}}/></div><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-5">{steps.map(step=><Link key={step.label} to={step.to} className={`flex min-h-12 items-center gap-2 rounded-xl border p-3 text-xs font-bold ${step.done?'border-teal-100 bg-teal-50 text-teal-800':'border-black/[.06] bg-cream text-ink/55'}`}>{step.done?<Check size={17}/>:<Circle size={17}/>} {step.label}</Link>)}</div></section>}
