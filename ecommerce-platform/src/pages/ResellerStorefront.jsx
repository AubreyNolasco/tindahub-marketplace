import { useEffect, useState } from 'react'
import { Facebook, MessageCircle, Package, Phone, ShieldCheck, ShoppingBag, Store, X } from 'lucide-react'
import { useParams } from 'react-router-dom'
import { supabase } from '../lib/supabase'
import Spinner from '../components/ui/Spinner'
import EmptyState from '../components/ui/EmptyState'
import { applyCampaignDiscount, getActiveCampaignDiscounts } from '../utils/campaigns'
import { isStoreOpen } from '../utils/storeHours'

export default function ResellerStorefront(){
  const{id,slug}=useParams();const[store,setStore]=useState(null),[products,setProducts]=useState([]),[loading,setLoading]=useState(true),[buying,setBuying]=useState(null)
  useEffect(()=>{(async()=>{setLoading(true);const storeResult=slug?await supabase.rpc('get_reseller_storefront_by_slug',{p_slug:slug}):await supabase.rpc('get_reseller_storefront',{p_reseller_id:id});const storefront=storeResult.data?.[0]||null;if(!storefront){setStore(null);setProducts([]);setLoading(false);return}const[listResult,discounts]=await Promise.all([supabase.from('reseller_storefront_products').select('product_id,products(*,merchant_profiles(business_name,store_open_time,store_close_time,auto_pause_outside_hours,store_timezone))').eq('reseller_id',storefront.id),getActiveCampaignDiscounts()]);setStore(storefront);setProducts((listResult.data||[]).map(row=>row.products).filter(product=>product?.is_active&&isStoreOpen(product.merchant_profiles)).map(product=>applyCampaignDiscount(product,discounts)));setLoading(false)})()},[id,slug])
  if(loading)return <div className="py-24"><Spinner/></div>
  if(!store)return <div className="mx-auto max-w-4xl p-6"><EmptyState icon={Store} title="Storefront unavailable" message="This Reseller storefront is not active."/></div>
  const digits=value=>String(value||'').replace(/\D/g,'')
  const channels=[store.storefront_facebook_url&&{label:'Facebook / Messenger',href:store.storefront_facebook_url,icon:Facebook},store.storefront_contact_number&&{label:`Call ${store.storefront_contact_number}`,href:`tel:${store.storefront_contact_number}`,icon:Phone},store.storefront_viber_number&&{label:'Open Viber',href:`viber://chat?number=${encodeURIComponent(store.storefront_viber_number)}`,icon:MessageCircle},store.storefront_whatsapp_number&&{label:'Open WhatsApp',href:`https://wa.me/${digits(store.storefront_whatsapp_number)}`,icon:MessageCircle}].filter(Boolean)
  return <div className="min-h-screen bg-[#f5f8f5] pb-12"><div className="relative h-44 overflow-hidden bg-gradient-to-br from-teal-950 to-teal-600 sm:h-64">{store.cover_url&&<img src={store.cover_url} alt="" className="h-full w-full object-cover"/>}<div className="absolute inset-0 bg-gradient-to-t from-teal-950/75 to-transparent"/></div><header className="relative mx-auto -mt-12 max-w-7xl px-3 sm:-mt-14 sm:px-6"><div className="rounded-3xl border border-black/[.06] bg-surface p-4 shadow-soft sm:flex sm:items-end sm:gap-5 sm:p-5"><div className="grid h-24 w-24 shrink-0 place-items-center overflow-hidden rounded-full border-4 border-white bg-teal-50 shadow-xl sm:h-28 sm:w-28">{store.avatar_url?<img src={store.avatar_url} alt={store.storefront_name||store.full_name} className="h-full w-full object-cover"/>:<Store size={36} className="text-teal-600"/>}</div><div className="mt-3 min-w-0 flex-1 sm:mt-0 sm:pb-2"><p className="text-[11px] font-bold uppercase tracking-[.12em] text-teal-600 sm:text-xs sm:tracking-[.16em]">Reseller Store</p><h1 className="mt-1 break-words font-display text-2xl font-bold leading-tight sm:text-3xl">{store.storefront_name||store.full_name}</h1>{store.reseller_bio&&<p className="mt-3 max-w-2xl break-words text-sm leading-6 text-ink/60">{store.reseller_bio}</p>}</div></div></header><main className="mx-auto mt-8 max-w-7xl px-2 sm:px-6"><div className="mb-4 px-2 sm:px-0"><h2 className="font-display text-xl font-bold">Available products</h2><p className="mt-1 text-sm text-ink/45">{products.length} product{products.length===1?'':'s'} selected by this Reseller</p></div>{products.length===0?<EmptyState icon={Package} title="No products listed yet"/>:<div className="grid grid-cols-2 gap-2 min-[360px]:grid-cols-4 sm:grid-cols-3 sm:gap-4 lg:grid-cols-4">{products.map(product=><article key={product.id} className="overflow-hidden rounded-xl border border-black/[.06] bg-surface shadow-card sm:rounded-2xl"><div className="aspect-square bg-teal-50">{product.images?.[0]?<img src={product.images[0]} alt={product.name} className="h-full w-full object-cover"/>:<Package className="m-auto h-full text-teal-300"/>}</div><div className="p-2 sm:p-4"><p className="truncate text-[8px] font-bold uppercase text-teal-700 sm:text-xs">{product.merchant_profiles?.business_name}</p><h3 className="mt-1 line-clamp-2 min-h-8 text-[10px] font-semibold sm:min-h-10 sm:text-sm">{product.name}</h3><p className="mt-2 font-display text-xs font-bold text-teal-700 sm:text-lg">₱{Number(product.price).toLocaleString()}</p><button onClick={()=>setBuying(product)} className="mt-2 flex w-full items-center justify-center gap-1 rounded-lg bg-teal-600 px-2 py-2 text-[9px] font-bold text-white sm:mt-3 sm:gap-2 sm:rounded-xl sm:text-sm"><ShoppingBag size={14}/> How to Buy</button></div></article>)}</div>}</main>{buying&&<div className="fixed inset-0 z-[100] grid place-items-center bg-scrim/65 p-3 backdrop-blur-sm" role="dialog" aria-modal="true"><div className="max-h-[92vh] w-full max-w-lg overflow-y-auto rounded-3xl bg-surface shadow-2xl"><header className="flex items-start justify-between bg-gradient-to-br from-teal-950 to-teal-700 p-5 text-white"><div><p className="text-xs font-bold uppercase tracking-wider text-mango-300">Customer buying guide</p><h2 className="mt-2 font-display text-xl font-bold">{buying.name}</h2></div><button onClick={()=>setBuying(null)} className="rounded-xl bg-white/10 p-2"><X size={18}/></button></header><div className="p-5"><ol className="space-y-3">{['Choose an available contact option below.','Tell the Reseller the product name and quantity you want.','Confirm the final price, delivery address, shipping fee, and payment method.','Pay only after you verify the Reseller details and complete order information.'].map((step,index)=><li key={step} className="flex gap-3 rounded-xl bg-cream p-3 text-sm leading-5 text-ink/65"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-full bg-teal-600 text-xs font-bold text-white">{index+1}</span>{step}</li>)}</ol><div className="mt-5"><h3 className="break-words font-bold">Contact {store.storefront_name}</h3>{channels.length?<div className="mt-3 grid gap-2">{channels.map(channel=>{const Icon=channel.icon;return <a key={channel.label} href={channel.href} target={channel.href.startsWith('http')?'_blank':undefined} rel="noreferrer" className="btn-secondary flex items-center justify-center gap-2 py-3"><Icon size={17}/>{channel.label}</a>})}</div>:<p className="mt-3 rounded-xl bg-mango-100 p-4 text-sm text-ink/60">This Reseller has not published a contact channel yet.</p>}</div><p className="mt-5 flex gap-2 rounded-xl bg-coral-100 p-3 text-xs leading-5 text-coral-700"><ShieldCheck size={16} className="mt-0.5 shrink-0"/>Never share your email OTP, password, banking OTP, or recovery code. Keep a copy of your confirmed order and payment receipt.</p></div></div></div>}</div>
}
