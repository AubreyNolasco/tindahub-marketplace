# Shopee/Lazada-Style Product Display — Progress & Handoff

**Status: built, deployed, live-verified on the deployed site (2026-08-08).**

Request: make the product display (what a Reseller sees browsing products to add to their storefront — `Catalog.jsx`, the shared `ProductCard.jsx`) look like Shopee/Lazada's product grid, while keeping JOM HUB's own architecture and brand identity per `TASK7/`. Scoped down from the original ask (which named both "product display" and "front page") to the Marketplace product-browsing surface specifically, after the owner confirmed the target audience was the Reseller browsing view — not a Landing-page rebrand, which `TASK7/PROJECT_ARCHITECTURE.md`'s "Marketplace must never replace the Business Panel, Landing Website must never replace the Marketplace" rule would have made a much bigger, architecturally riskier change for no stated reason.

## What was built

- **`ProductCard.jsx`** (shared by `Catalog.jsx`, `MerchantStore.jsx`, and `Reseller/StorefrontProducts.jsx` — one component, so all three stay visually consistent per `UI_UX_GUIDELINES.md`'s reuse rule):
  - Discount badge moved from a bottom-right pill to a **corner ribbon, top-left** — the Shopee/Lazada flash-sale-badge position shoppers scan for first.
  - **Strikethrough original price** next to the effective discounted price, shown only for non-reseller viewers (a Reseller's displayed price is their wholesale buying cost, not a retail discount, so mixing in the merchant's retail strikethrough there would misrepresent what they're actually paying — kept the existing reseller pricing logic untouched).
  - Price now computed via `getUnitPrice()` — the same helper `Cart`/`Checkout` already use — instead of showing the raw undiscounted `product.price` for non-reseller viewers, so the badge percentage and the price shown can never disagree with each other.
  - New **star rating + review count + sold count row**, reusing the existing `StarRating.jsx` component (previously only used on `ProductDetail.jsx`). Hidden entirely when a product has no reviews/sales yet, rather than showing "0.0 (0) · 0 sold" clutter on every card.
- **`get_product_sold_counts()` RPC** (`20260808000300_product_sold_counts.sql`): `order_items` is participant-scoped RLS (order owner/merchant/staff only), so a client-side "X sold" aggregate isn't directly queryable by an ordinary shopper. This RPC exposes only a per-product completed-order unit count — no order, customer, or pricing detail — the same public-safe-aggregate posture `product_reviews`' own `for select using (true)` policy already has.
- **`Catalog.jsx`**:
  - Bulk-fetches the ratings aggregate (`product_reviews`, already public-read) and the new sold-count RPC alongside the product query — one join-not-loop query each, batched with the page's existing `Promise.all` pattern, not a per-product loop.
  - New **horizontal category icon row** below the hero search bar — the Shopee/Lazada category-shortcut pattern. Client-side keyword-to-`lucide-react`-icon mapping (`categories` has no icon column, so this is presentational only, no schema change); clicking a category reuses the existing `activeCategory` filter state, no new filtering logic.
  - Grid density bumped slightly at very large screens (`2xl:grid-cols-5`) for more of the "many small tiles" feel; every existing mobile/tablet breakpoint left untouched.
- **`compactCount()`** added to `utils/format.js` (`Intl.NumberFormat` compact notation — "1.2K sold") since the codebase's `format.js` didn't have one and this is the second place it'd be useful (product cards now, review counts later if ever needed).

## What was deliberately not touched

- No pricing, checkout, order, wallet, or RLS logic changed anywhere — this is a display-layer pass only, reusing existing pricing helpers (`getUnitPrice`, `getResellerProfitEstimate`) rather than introducing a second price-computation path.
- No brand/color rebrand — every new element uses the existing `teal`/`mango`/`coral` design tokens (`coral-600` for the discount ribbon and sale price reads as a natural "sale" accent within the existing palette, not a new color introduced for this).
- `ProductDetail.jsx` was not restyled to match — this pass targeted the grid/browsing view specifically, since that's the surface most directly comparable to Shopee/Lazada's product feed. Flagged as a natural follow-up if the single-product page should get the same treatment (price/rating layout) later.
- `MerchantStore.jsx` and `Reseller/StorefrontProducts.jsx` inherit the new `ProductCard` look automatically (shared component) but were not wired up to fetch the ratings/sold aggregates themselves — their cards render correctly, just without the rating/sold row, since that data isn't fetched on those pages yet. Not a bug, just unfinished scope; wiring the same bulk-fetch pattern from `Catalog.jsx` into those two pages is the natural next step if wanted there too.

## Verification

- `npx eslint .` — 0 errors/warnings. `npm run build` — clean. `npm test` — 36/36 passing.
- Migration applied (`get_product_sold_counts` confirmed callable).
- **Live-verified on the deployed site**, in Reseller-demo mode (the exact audience this was built for): category icon row renders and is clickable, discount ribbons show correctly on the campaign products, prices render in the sale-accent color, reseller "Sell/Profit" panel still renders unchanged below. No console errors on page load. Rating/sold row correctly stays hidden for these particular demo products (zero reviews, zero completed orders on this account) rather than showing empty placeholders — confirmed this is the intended graceful-degradation behavior, not a bug.
- Mobile-width visual check **not done this pass** — same tooling limitation recorded in `TASK9.md` (the browser automation's resize tool doesn't actually change the rendered viewport in this environment). The new elements use the same mobile-first Tailwind patterns (`flex-col` defaults, no fixed widths, existing breakpoint scale) already verified structurally sound for the TASK8 work; not yet looked at on an actual narrow screen.

## Follow-up, same day: sample catalog with visible ratings/sold data

The live check above used a fresh catalog with zero reviews and zero completed orders, so the new rating/sold-count row never actually rendered in that pass — there was nothing to show. The owner asked to be able to see the redesign properly, with sample images and social-proof data, plus an Admin-controllable way to add/refresh and delete that sample data on demand (not a one-time seed).

**Built:**
- `admin_seed_sample_catalog(jsonb)` / `admin_delete_sample_catalog()` — admin-only RPC pair (`20260808000400_admin_sample_catalog.sql`), same hardcoded-admin-email-check precedent `reset_admin_demo_data()` already established, scoped to the Admin Demo Merchant account. Product data (name, price, images, packed dimensions, etc.) is passed in from the client, sourced from the existing `src/config/sampleProducts.js` — the single place that config already lived (also used by `Merchant/Products.jsx`'s own "seed samples" button), not duplicated into SQL.
- Seeding also creates, per sample product: one synthetic **completed** order + order item (a "sold count" anchor — `get_product_sold_counts()` just sums `order_items.quantity` from completed orders, so this alone is enough to produce a realistic-looking sold badge) and one **rating** (`product_reviews`).
- **Real constraint respected, not bypassed**: `product_reviews` inserts require `profiles.role = 'reseller'` at insert time (`prepare_product_review()`'s existing `RESELLER_ONLY` check — a genuine business rule, not incidental). Rather than weakening that check to make seeding easier, the seed function briefly calls the same `switch_role_for_demo('reseller')` / `switch_back_to_admin()` pair the "Admin demo mode" UI already uses, from inside its own transaction — so if anything in the function raises, the *entire* transaction (including the role switch) rolls back. This can't leave the account stuck mid-switch the way a manual multi-step process could (which is exactly the failure mode `TASKS.md`'s 2026-08-07 incident writeup describes for the old manual flow).
- Delete removes the tagged sample orders first (`order_items`, `product_reviews`, and this session's `purchase_orders` all cascade off `orders.id` — verified via `pg_constraint` before relying on it, not assumed), then the sample products themselves.
- New **"Sample catalog"** panel on `Admin/TestAccounts.jsx`: "Add / Refresh Samples" and "Delete Samples" buttons, right below the existing "Admin demo mode" section. Idempotent — re-running Add/Refresh updates the existing 4 sample products and skips re-seeding a review/order that already exists for a product, rather than duplicating them.

**Live-verified**: clicked "Add / Refresh Samples" as Admin (no role-switch needed from the UI side — the button works directly from the Admin dashboard). Confirmed via direct query afterward that `profiles.role` returned cleanly to `'admin'` with `previous_role` null (no stuck state across all 4 internal role-switch cycles), and that all 4 sample products now have exactly one review and one sold-quantity row each. Reloaded `/catalog` and confirmed the cards now show real data: `★★★★★ 5.0 · 51 sold`, `★★★★ 4.0 · 23 sold`, `★★★★ 4.0 · 25 sold`, `★★★★ 4.0 · 75 sold` — the rating/sold-count row this session's redesign added is now actually visible, not just structurally present.

**Not done**: did not live-click "Delete Samples" — it triggers a native browser `confirm()` dialog, which this environment's browser-automation guidance says to avoid triggering (risk of blocking the session). Its logic is simpler than the seed function (two straightforward `DELETE` statements, no role-switching, cascade behavior already confirmed via `pg_constraint` and already exercised manually earlier this session during TASK8's own test-order cleanup) and lower-risk by comparison, so this is a reasoned gap, not an oversight — flagged for whoever next has a moment to click it once by hand. Samples were deliberately left in place afterward (not deleted) so the owner can see them on the live site.

## Follow-up, same day: matched against a reference mockup, expanded the sample catalog

The owner shared a reference image (`JOM HUB Marketplace UI_UX Showcase.png`, project root) showing the full intended direction across six surfaces: Landing page, Marketplace front page (mega-sale banner, flash sale countdown, trust badges), Store page, Product Detail page, Category/Listing page, and Business Panel dashboard. That's a much bigger scope than one pass — sequenced it instead of attempting all six at once, starting with the Marketplace front page since that's the one repeatedly asked about, and flagged Store/Product-Detail/Dashboard as explicit follow-ups rather than silently skipping them.

**Built (Marketplace front page, `Catalog.jsx`):**
- A "MEGA SALE" banner sized to the current top active-campaign discount, reusing the campaign data already being fetched — no new query.
- Renamed "On campaign right now" to "**Flash Sale**" and gave it a live countdown by reusing `CampaignCountdown.jsx` (already existed, previously only used on `ProductDetail.jsx`) — reuse-first, not a new timer component.
- A trust-badge row — deliberately **not** a copy of generic marketplace badge copy. "Free Shipping" isn't something this platform promises (shipping here is receiver-pays-on-delivery or reseller-arranged, per `TASK7/BUSINESS_RULES.md`), so the badges instead state three things JOM HUB actually does: wallet-escrow payments, merchant verification before listing, and real-time order tracking.

**Then, expanded the sample catalog** so the new design has enough variety to actually look populated (the original 4 samples were all grocery/home items with no `category_id` set at all — every category filter except "All" showed zero results):
- Added **Wireless Earbuds Pro** (Electronics & Gadgets) and **Everyday Cotton Tee 3-Pack** (Fashion & Apparel) to `src/config/sampleProducts.js` — the same file both `admin_seed_sample_catalog()` and the pre-existing `Merchant/Products.jsx` seeding button already read from.
- Assigned `category_id` to all 6 sample products (previously none had one).
- New product images are external HTTPS URLs (`images.unsplash.com`, the same CDN the Admin homepage banner editor's own default hero image already uses) — **verified reachable and visually confirmed showing the correct product** (downloaded and looked at both images) before using them, not linked blind.
- `admin_seed_sample_catalog()` updated via `CREATE OR REPLACE` in a new migration (`20260808000500_admin_sample_catalog_category.sql`) to read/write `category_id` — forward-only per `BUSINESS_RULES.md`, the original migration file wasn't touched.

**Live-verified**: clicked "Add / Refresh Samples" — role cycled and restored cleanly again (checked via query, no stuck state). Reloaded `/catalog`: MEGA SALE banner, trust badges, and Flash Sale countdown all render correctly; the t-shirt and earbuds show up in the Flash Sale row alongside the original 4. Clicked the "Electronics & Gadgets" category icon and confirmed it now correctly filters to exactly one real result — Wireless Earbuds Pro, ★★★★★ 5.0 · 72 sold, ₱1,143.12 struck through ₱1,299.00 — proving the category row is a functional filter now, not just decoration. No console errors on load.

**Still open** (explicitly sequenced, not forgotten): Store page redesign (`MerchantStore.jsx`), Product Detail page redesign (`ProductDetail.jsx`), Category/Listing sidebar redesign to match the mockup's tree-style categories and star-rating filter, and the Business Panel dashboard redesign (a separate module per `TASK7/PROJECT_ARCHITECTURE.md` — doesn't block or depend on the Marketplace work above). Mobile-width visual check still not done, same recorded tooling limitation.

## Follow-up, same day: the three items above, done (told to keep going without pausing between them)

**`ProductDetail.jsx`**:
- Thumbnail strip + main image, only rendering when a product actually has more than one image (most sample products still have one, so this degrades to exactly the previous single-image layout for them — no regression for the common case).
- Rating row now also shows sold count (new `get_product_sold_counts` call for the single product being viewed); the main price line shows the strikethrough original price the same way `ProductCard` already does, instead of only inside the separate campaign callout box further down.
- Added **Buy Now** next to **Add to Cart** — same `QuickAddModal` flow, a `buyNow` flag makes `confirmAdd()` navigate to `/cart` afterward instead of just closing the modal.

**`Catalog.jsx` rating filter**: 4/3/2/1-stars-and-up options in the existing `FilterPanel`, reusing `StarRating`. Applied client-side (`visibleProducts`) since `avg_rating` is a post-fetch aggregate, not a queryable column — same reasoning as the price-range filter already being client-visible state, just filtered after load instead of in the query. Did **not** attempt the mockup's tree-style expandable categories — this app's `categories` table is flat (no parent/child column), so a tree UI would have nothing real to expand; flagged rather than faked with dummy nesting.

**`MerchantStore.jsx`**: store header redesign with an aggregate rating (weighted across the store's own products by each product's review count — a store with one 5-star review and one 1-review-count product shouldn't out-rank a store with a hundred consistent 4-star reviews, hence weighting by count, not a flat average of averages), an "Official Store" badge for approved merchants, and a "Best Selling Products" section (top 5 by `sold_count`) ahead of the existing campaign/all-products grids. Trust badges reworded the same honest way as `Catalog.jsx`'s — no "100% Authentic" / "7-Day Returns" claims this platform doesn't make. **Deliberately did not** add a follower count or Follow button — no followers feature exists anywhere in this codebase (table, RLS, follow/unfollow RPC would all be new), so that's a real feature request, not a display change; flagged rather than stubbed with fake numbers.

**Live-verified**, all three, on the deployed site:
- `/merchant-store/<Admin Demo Merchant id>` — header shows "★★★★★ 4.5 (6 reviews) · 6 products", 3 trust badges, and Best Selling Products correctly sorted (75, 72, 51, 45, 25 sold, descending).
- Clicked into "Wireless Earbuds Pro" — rating+sold row and strikethrough price render correctly on the product page.
- Rating filter: clicking "4★ & up" correctly applied a `Rating: 4★ & up` badge and kept all 6 products visible (every sample product genuinely has ≥4.0 average, so that's the correct result, not a no-op).
- No console errors on any of the three pages.
- One tooling hiccup during this verification pass: the screenshot capture tool returned blank frames partway through (unrelated to the app — confirmed by cross-checking the same page state with `get_page_text`, which returned full, correct content each time). Verification continued via text extraction and element-targeted interaction instead of fighting the screenshot tool further.

## Business Panel dashboard: checked structure (already good), then found a real polish gap and fixed it

Read `Merchant/MerchantDashboard.jsx` and `Reseller/ResellerDashboard.jsx` in full before touching anything. Structurally, both already matched the mockup's shape closely — `PageHeader` welcome greeting, a 4-card `StatCard` grid, an `AnalyticsPanel` 14-day trend chart, a recent-activity list with `Badge` status — leftover quality from `TASK2.md`'s prior UI/UX pass, not something to redo.

Initially recorded this as "no changes needed." The owner pushed back — asked to actually look for what could be improved rather than stop at "already matches." Looking again at `StatCard.jsx` itself (not just the dashboards using it) found a real, concrete gap: the component already supports an optional `delta` prop (the mockup's "+18.5% vs last month" chip, up/down arrow + colored badge) and an optional `trend` prop (a faint sparkline, the same chart primitives `AnalyticsPanel` already uses) — **neither dashboard was passing them in.** The capability existed; it just wasn't wired up.

**Fixed**: added `periodDelta()` to both dashboards — a display-only 30-day-vs-previous-30-day comparison on the primary sales/purchases stat card, computed from orders data each dashboard already fetches (no new query), and wired the same 14-day trend array already built for the chart below it into that card's sparkline. Returns `null` (chip stays hidden, not a misleading `+100%`/`-100%`) when there's no prior-period data to compare against yet — confirmed this behaves correctly live: the Admin Demo Merchant's 6 sample orders were all created today, so there's nothing in the prior 30-60 day window, and the chip correctly doesn't render rather than showing a fabricated number.

**Live-verified**: switched to Merchant mode, confirmed the dashboard renders ₱152,905 gross sales across 6 orders, the sales trend chart populated correctly, no console errors, delta chip correctly absent (no comparison data, as expected) rather than broken. Switched back to Admin afterward and confirmed the role restored cleanly (`role = 'admin'`, `previous_role = null`, no stuck state).

`Admin/AdminDashboard.jsx` wasn't restyled to match the mockup — it serves a structurally different purpose (platform-wide oversight, not "my own store's sales") — but grepped the codebase for every `StatCard` consumer to check for the same unused-props gap, and found it had it too: the "Gross marketplace value" card wasn't passing `delta`/`trend` either, even though `revenueTrend` was already computed for the chart right below it. Fixed the same way, same `periodDelta()` pattern. All three (and only three — confirmed via grep) `StatCard` consumers in the codebase are now consistent.

Live-verified on `/admin`: ₱152,905 gross marketplace value, 14-day chart populated, delta chip correctly absent (no prior-period data), Top Performers leaderboard correct, no console errors. Caught and fixed one real thing along the way: found the account sitting in Reseller demo mode from an earlier step in this session (not stuck — `previous_role` was still populated — just left mid-demo), switched it back to Admin via the UI's own "Back to Admin" button and confirmed clean via query before continuing, rather than working around it or ignoring it.

**This closes out the full sequence** from the reference mockup: Marketplace front page (banner/flash-sale/trust badges/categories), Product Detail, Category filter (rating), Store page, and Business Panel dashboard — five of five, with the honest exceptions recorded along the way (no followers feature, no category tree, mobile-width still unverified visually).

## Follow-up, same day: `ResellerStorefront.jsx` had the same gap as `MerchantStore.jsx`, before this session

Kept looking rather than stopping at "five of five" — the owner's instruction was to keep going as long as there were real gaps left, not to stop at the mockup's five named surfaces. `ResellerStorefront.jsx` (the public storefront page a reseller's own customers actually land on and buy from — arguably the highest-stakes surface of all, since it's real customer-facing checkout traffic, not an internal dashboard) shared `ProductCard`'s old layout (top-right discount badge, no rating/sold row) because — unlike `Catalog.jsx` and `MerchantStore.jsx`, which were both updated earlier this session to bulk-fetch ratings and sold counts — this page had never been touched to fetch that data at all.

**Fixed**: added the same bulk `product_reviews` fetch + `get_product_sold_counts()` RPC call pattern already used on `Catalog.jsx`/`MerchantStore.jsx` into `ResellerStorefront.jsx`'s data-load effect, merging `avg_rating`/`review_count`/`sold_count` onto each product. Moved the discount badge from top-right to the same top-left coral-600 ribbon used everywhere else, and added the rating+sold row (via `StarRating` + `compactCount`) to both the product grid card and the product-detail modal view on this page — bringing it to full visual parity with the rest of the redesign instead of being the one surface left behind.

**Live-verified** on the deployed site (commit `31a9e7e`): navigating to `/reseller-store/<Admin Demo Merchant id>` while the account was still in Admin role correctly showed "Storefront unavailable" (expected — `get_reseller_storefront` requires the active reseller role, not a bug). Switched to Reseller demo mode, reloaded, and confirmed via `get_page_text`: `-12% / ADMIN DEMO MERCHANT / Citrus Dishwashing Liquid 1L / 4.0 / 75 sold / ₱127.6 ₱145 / Order now` — discount ribbon, rating, sold count, and strikethrough pricing all rendering correctly on the actual customer-facing storefront. No console errors. Switched back to Admin via the "Back to Admin" button and confirmed via direct query afterward (`role = 'admin'`, `previous_role = null`) — no stuck demo-mode state.

With this, all `ProductCard`-based surfaces in the app (`Catalog.jsx`, `MerchantStore.jsx`, `ResellerStorefront.jsx`) are now visually and functionally consistent — same badge position, same pricing logic, same rating/sold-count treatment — rather than three components that merely share a card component but display different data depending on which page happened to fetch it.

## Follow-up, same day: real follower feature added to `MerchantStore.jsx`, scoped to where it's real

Swept the codebase for other genuine gaps before stopping: every remaining shopper-facing product surface now matches (confirmed via grep — no other page renders its own product tiles outside `ProductCard`, and `Reseller/StorefrontProducts.jsx`, the one page that does, is the reseller's own backend curation list, not a shopper view, so it correctly stays plain). That left two items previously flagged as real feature requests rather than polish: a follower system and a category tree. Asked before building either, since both mean new schema/RLS, not a display tweak. Given the go-ahead for the follower system.

**Scoped deliberately to one direction**: Reseller-follows-Merchant on `MerchantStore.jsx` only, not `ResellerStorefront.jsx`. Checked `App.jsx`'s route table first — `/merchant-store/:id` is behind `ProtectedRoute allowedRoles={['reseller','merchant','admin','staff']}` (real, logged-in platform accounts), while `/reseller-store/:id` and `/store/:slug` are unauthenticated public routes with no `useAuth` calls at all (confirmed by grep) — that page's visitors are guest customers reached via the Facebook/Viber/WhatsApp contact fields already built in `StorefrontProducts.jsx`, not platform accounts. There's no real identity to attach a "follow" to there, so building it would mean fabricating an account system nobody asked for.

**Built**:
- `merchant_followers` table (`20260808000600_merchant_followers.sql`) — `reseller_id`/`merchant_id` pair, unique constraint, RLS matching the `product_reviews` precedent (owner read/write, `is_admin()`/`has_admin_permission('reviews')` staff override) — checked `product_reviews`' actual live policies via `pg_policies` before writing these, not guessed.
- `get_merchant_follow_stats(merchant_id)` — security-definer, returns only a count and the caller's own follow state, never the list of who follows whom (same public-safe-aggregate posture as `get_product_sold_counts`).
- `toggle_merchant_follow(merchant_id)` — security-definer, blocks non-resellers (a merchant/admin/staff browsing another merchant's store isn't a "customer" of that store in this app's model) and blocks self-follow.
- `MerchantStore.jsx`: live follower count in the header meta row, a Follow/Following button (heart icon, filled when following) next to Chat — visible only to resellers viewing someone else's store — with optimistic toggle and rollback on RPC failure.

**Verification**:
- `npx eslint` clean, `npm run build` clean, `npm test` 36/36 passing.
- Confirmed `is_admin()`/`has_admin_permission()` signatures against the live database before using them in the migration, rather than assuming. Migration applied cleanly via `supabase db push`.
- **Noticed and paused on unexpected state**: mid-verification, the shared demo account's role flipped from `admin` to `reseller` between two checks moments apart, with no action from me. Rather than assume it was safe to grab for testing, flagged it and asked — confirmed to be the owner actively using demo mode, not a stuck-state bug.
- Directly verified the RPC's auth guard: called `toggle_merchant_follow()` via raw SQL (no `auth.uid()` in that context) and confirmed it correctly raises `Only resellers can follow merchant stores.` rather than silently succeeding.
- Live-verified on the deployed site (`tindahub-marketplace.vercel.app`, reseller mode): merchant store page renders `4.5 (6 reviews) · 6 products · 0 followers`, Follow button correctly absent when viewing your own store (self-follow hidden at the UI layer, matching the RPC's own guard), no console errors.
- **Not exercised live**: a full follow → count increments → unfollow → count decrements round-trip, because the system currently has exactly one merchant account (the same dual-identity demo account), and self-follow is correctly blocked. Testing the happy path would require impersonating the one other real reseller account (Ferdie Cay) to create a follow row attributed to them — declined to do that without asking, since it means fabricating activity on a real person's account rather than a synthetic demo one. The insert/delete/count logic itself is simple, standard SQL (mirrors the already-proven `product_reviews` RLS pattern) and was read and reasoned through directly rather than assumed correct.

## Follow-up, same day: category tree — the last flagged item, built

The other item asked about alongside the follower system: a real parent/child category structure, since the `categories` table was flat (no `parent_id`) and the mockup's category filter shows an expandable tree. Given the go-ahead for both.

**Built**:
- `categories.parent_id` (`20260808000700_category_tree.sql`) — nullable, `ON DELETE SET NULL` (same convention `products.category_id` already uses, checked via `pg_constraint` before matching it). Depth capped at one level by `enforce_category_tree_depth()`, a trigger that blocks both directions of a would-be 3-level tree: a subcategory can't become a parent, and a category that already has children can't become someone else's child.
- `Admin/Categories.jsx`: parent picker on the add form (top-level categories only, since children can't have children), nested tree rendering (children indented under their parent with a delete action each) instead of a flat list.
- `Catalog.jsx`: the sidebar filter's category section is now a real expandable tree — a chevron toggles a top-level category's children open, auto-expanded while searching if any child matches. Selecting a parent category now matches products filed directly under it **or** under any of its children (`query.in('category_id', [parent, ...childIds])` instead of a plain `.eq`) — otherwise a merchant tagging products under "Phones & Accessories" would make them invisible under "Electronics & Gadgets," which defeats the point of a tree. The top horizontal shortcut row was narrowed to top-level categories only, so it doesn't end up duplicating the sidebar tree once subcategories exist.
- `Merchant/ProductForm.jsx`: the category `<select>` now groups subcategories under their parent via `<optgroup>` instead of a flat alphabetical list, so a merchant tagging a product can see the hierarchy they're picking from.
- Didn't touch `Merchant/Vouchers.jsx` / `Admin/Vouchers.jsx`'s own category pickers — they still work correctly (any category, parent or child, remains individually selectable), just without the `<optgroup>` grouping; a cosmetic gap, not a functional one, left for a later pass rather than touched speculatively.

**Verification**:
- `npx eslint`, `npm run build`, `npm test` (36/36) all clean.
- Migration applied via `supabase db push`.
- **Paused before touching the shared demo account's role a second time**: it was still in Reseller mode from the previous phase (the owner's own active session). Asked before switching it to Admin to test the new admin UI, rather than assuming the earlier go-ahead extended automatically to a new action.
- Live-verified on the deployed site: created "Phones & Accessories" as a subcategory of "Electronics & Gadgets" in `Admin/Categories.jsx` — rendered correctly nested, no console errors. Reassigned "Wireless Earbuds Pro" to the new subcategory via `ProductForm.jsx`'s new `<optgroup>` picker (confirmed the dropdown showed "Electronics & Gadgets (general)" grouped correctly before the change). On `/catalog`, selecting the parent "Electronics & Gadgets" filter correctly returned the product ("1 product found") even though it's tagged under the child subcategory — confirming the parent-matches-children query logic works, not just renders. Expanding the tree's chevron correctly revealed "Phones & Accessories" nested under it. No console errors on any step.
- Restored the demo account to Reseller mode afterward (`role = 'reseller', previous_role = 'admin'`) — the exact state it was in before this verification pass touched it, confirmed via query.

This closes out both items that were flagged after the original five-surface mockup sequence as real feature requests rather than display gaps — follower system and category tree are both now built, deployed, and verified.

## Follow-up, same day: the follower feature had a gap of its own — no notification

Kept sweeping after closing out the mockup sequence's two flagged items, and this time found a real gap in something built *this session*, not the original mockup. The app already has a `notifications` table and `create_notification()` RPC (`20260804000100_integration_scaffolding.sql`), used by `review_campaign_submission()` and `run_campaign_scheduler()` for exactly the class of event that has "no natural source row to derive a notification from" (that migration's own comment) — campaign approval/rejection is one example, and a merchant getting followed is structurally the same kind of event. `toggle_merchant_follow()` never wired into it, so a merchant had no way to know they'd been followed short of manually re-checking their own store page.

**Fixed** (`20260808000800_merchant_follow_notification.sql`, `CREATE OR REPLACE` on the same function): on a successful follow (not unfollow — matching how every other producer in this table only surfaces positive/actionable events), the function now calls `create_notification()` directly, same pattern `review_campaign_submission()` already established (a `security definer` function calling another `security definer` function directly, not through PostreSQL's client-facing RPC grant, which is intentionally `service_role`-only). Notification links to `/merchant-store/<merchant_id>` so clicking it lands the merchant right on their own store page where the new follower count is visible. `RoleNotifications.jsx` got a `'follow' → Heart` icon mapping alongside the existing `'voucher' → Ticket` one.

**Verification**: `npx eslint`, `npm run build`, `npm test` (36/36) clean. Migration applied via `supabase db push` — a syntax/type error in the `plpgsql` body would have failed the push outright, so a clean push is itself a real compile check, not just "no error shown." Re-ran the same direct-SQL auth-guard check as before (`toggle_merchant_follow()` called with no `auth.uid()` in a raw SQL session) to catch any regression from the `CREATE OR REPLACE` — still correctly raises `Only resellers can follow merchant stores.` **Did not** exercise the actual notification-insert happy path live, for the same reason as the follow toggle itself: the system has one merchant account and one other real reseller account (Ferdie Cay), and generating a real follow + a real notification attributed to that account without asking isn't something to do just to close a checkbox. The insert itself is a one-line `perform create_notification(...)` call using an already-proven RPC with already-proven parameter shapes (four other call sites in `20260807000200_campaign_notifications.sql` use the identical signature) — read and reasoned through, not guessed.
