# Enterprise Promotion Management System — Progress & Handoff

**Status: All 13 phases complete and deployed live. See "Open item" at the bottom — full browser checkout still hasn't been exercised end-to-end in any session, only verified via direct SQL against the live database.**

Upgrading the existing manual, merchant-wide, admin-only Campaign module (`campaigns` + `merchant_campaigns` tables, `src/pages/Admin/Campaigns.jsx`, `src/pages/Merchant/Campaigns.jsx`, `src/utils/campaigns.js`, `src/utils/pricing.js`) into a per-product, automated, validated, scheduled promotion engine with vouchers, banners, badges, bulk import/export, and checkout-time enforcement — while reusing the existing architecture (RLS, security-definer RPCs, `revenue_settings`/`quote_order`/`place_order` pattern, Admin `DataTable`/`EmptyState`/`Spinner` UI kit, `merchant_status`-style enum + trigger notification pattern) rather than replacing it.

## Phase 1 — Analysis (complete)

Full architecture read-through of: `campaigns_migration.sql`, `automatic_calendar_campaigns_migration.sql`, `src/pages/Admin/Campaigns.jsx`, `src/pages/Merchant/Campaigns.jsx`, `src/utils/campaigns.js`, `src/utils/pricing.js`, `src/components/product/ProductCard.jsx`, `src/contexts/CartContext.jsx`, `src/pages/Reseller/Checkout.jsx`, the live `quote_order()` and `place_order()` function bodies (queried directly from the linked database — the on-disk migration history for `place_order` is fragmented across 8 loose, non-timestamped `.sql` files, so the live `pg_proc` definition was used as the source of truth), and the `products` table schema.

**Most important finding — a live, currently-shipping bug, not just a gap:** campaign discounts are computed and displayed **client-side only** (`utils/campaigns.js` → `campaign_discount_percent` on the cart item → `utils/pricing.js`'s `getUnitPrice`). The actual money-moving RPC, `place_order()` (called via `place_receiver_shipping_order`/`place_customer_receiver_shipping_order`), recomputes `unit_price` from `product.price`/`wholesale_price` + `discount_tiers` only — it never reads `campaigns`, `merchant_campaigns`, or any campaign discount. **A shopper sees "20% OFF" in their cart and is charged full price at checkout.** A `product_unit_price()` SQL function exists that *does* include the campaign discount, but it is dead code — nothing calls it. This must be fixed as part of Phase 9 (Automatic Promotion Engine) at the latest, and is flagged as a candidate for an earlier, isolated hotfix given it's a real financial-trust bug independent of the rest of this project.

**Other findings, delivered to the user in full in-chat (not duplicated here — see conversation):**
- Campaigns today are whole-merchant flat-percent-off (join = every active product in the store gets the same % off), not per-product — no product selection, no per-product campaign price, no SKU-level opt-in/out.
- Joining a campaign is instant/automatic with no submission, no approval workflow, no validation (stock, pricing floor, conflicting campaigns) — "mostly manual" in the request actually undersells it; there's no merchant-submission concept to be manual *about* yet.
- No Voucher module exists anywhere in the codebase (grepped `voucher|promo_code|discount_code` across `src/` — zero matches). Phase 10 is net-new, not an improvement of something existing.
- No banners, no homepage featured-campaign section, no countdown timers, no category-page ribbons, no merchant-store campaign collection. The only existing visual surface is a small corner badge on `ProductCard.jsx` ("CAMPAIGN 20% OFF"), derived from the same client-only discount number.
- No real scheduler. `ensure_recurring_campaigns()` (which seeds the Payday/Double Day recurring campaigns) is only invoked lazily, client-side, every time an admin happens to open `Admin/Campaigns.jsx` — not on a `pg_cron` schedule. If no admin opens that page, next month's calendar campaigns silently never get created.
- `products` table has zero campaign-linkage columns — confirms campaigns are merchant-level only today, consistent with the above.
- Reusable patterns identified for Phases 2–13: the `merchant_status` enum (`pending/approved/rejected/suspended`) + trigger-notify-on-transition pattern already used for merchant approval is the right template for the new `Draft/Pending/Approved/Active/Paused/Rejected/Expired/Archived` submission status; `quote_order()`/`place_order()`'s security-definer, server-recomputes-everything pattern is the right template for closing the pricing-bug gap and for the Phase 9 promotion engine; the generic Admin `DataTable` + `Integrations.jsx`-style credential/config form pattern (already proven for 10+ provider integrations) is a good fit for Phase 13's rule/pricing configuration screens instead of building bespoke forms.

## Phase 2 — Merchant Campaign Center (complete, deployed, tested live)

**Design decision (user deferred to recommendation):** the existing whole-store "instant join" flow (`campaigns`/`merchant_campaigns`, `Merchant/Campaigns.jsx`'s "Join campaign" button) is left completely untouched and keeps working exactly as before — used going forward for the system-generated recurring campaigns (Payday/Double Day), which are low-stakes fixed discounts with no need for per-product control. The new per-product submission+approval flow is additive, for merchant-initiated campaigns needing pricing control and review.

**Built:**
- Migration `20260806001200_campaign_products.sql` — new `campaign_submission_status` enum (`draft/pending/approved/active/paused/rejected/expired/archived`), new `campaign_products` table (campaign_id, merchant_id, product_id, campaign_price, status, validation_errors, rejection_reason, submitted_at/reviewed_at/reviewed_by), additive `requires_approval`/`min_discount_percent`/`max_discount_percent` columns on `campaigns` (existing rows unaffected — all default/nullable), RLS mirroring the existing `merchant_campaigns` policy shape.
- RPCs: `submit_campaign_product()` (runs Phase-3-style inline validation — product ownership/active/stock, campaign schedule, pricing floor/ceiling, overlapping-campaign conflict check — before inserting/updating, returns per-field validation errors instead of one opaque exception), `withdraw_campaign_submission()`, `review_campaign_submission()` (admin-only approve/reject).
- `Merchant/Campaigns.jsx` — added a "Submit individual products" collapsible panel per campaign card: product picker, campaign price input, submission list with status badges, edit price / withdraw actions, inline validation-error display. Existing "Join campaign (whole store)" button untouched.
- `Admin/Campaigns.jsx` — added a "Pending product submissions" panel above the existing campaign grid, with Approve/Reject actions. Existing campaign create/toggle UI untouched.
- Migration `20260806001300_fix_review_campaign_submission_enum_cast.sql` — hotfix found during live testing (see below).

**Live-tested end-to-end** using the "Admin Demo Merchant" account (same login, dual admin+merchant identity, already set up in this project for exactly this kind of testing) against the deployed production site, with a temporary SQL-seeded test product (cleaned up after):
1. Submitted a product at ₱85 (from ₱100) to the "8.8 Double Day Sale" campaign → correctly landed in **Pending** (campaign has no `min/max_discount_percent` set, so no validation errors; `requires_approval` defaults `true`).
2. **Bug found and fixed live:** `review_campaign_submission()`'s `UPDATE ... SET status = (CASE WHEN p_approve THEN 'approved' ELSE 'rejected' END)` failed with `42804 column "status" is of type campaign_submission_status but expression is of type text` — a `CASE` expression embedded directly in an `UPDATE`'s `SET` clause doesn't get the implicit enum cast that a typed PL/pgSQL variable assignment gets (confirmed `submit_campaign_product`'s `v_status := CASE ...` into a `campaign_submission_status`-typed variable was unaffected). Fixed with an explicit `::public.campaign_submission_status` cast, redeployed, reverified.
3. Re-tested Approve → **Approved** on both Admin and Merchant screens.
4. Tested Withdraw → correctly disappears from the merchant's active submission list (status → `archived`).
5. Cleaned up the temporary test product and submission row from the database.

## Incident found & fixed during Phase 8/9 live testing (unrelated to the campaign feature itself)

While testing, the owner's own account (`bc37187b-...`, `nolascoaubrey32@gmail.com`) was found stuck with `profiles.role = 'reseller'` instead of `'admin'` — the account was left mid-way through the existing "demo role switch" feature (`switch_role_for_demo()`/`switch_back_to_admin()`, see `20260729000600_admin_demo_role_switch.sql`), most likely from a stray click earlier in this same testing session that entered "Reseller Mode (Admin demo)" without the matching "Back to Admin" completing. `profiles.previous_role` still correctly held `'admin'`, so this was a safe, exact restoration (`role = previous_role, previous_role = null` — the same update `switch_back_to_admin()` itself performs), not a guess. Verified live afterward: Admin Center sidebar and all admin pages (including the new Campaign Management "Export all" button) working normally again. Flagging this here per `TASK7/CLAUDE_INSTRUCTIONS.md`'s documentation rule, even though it wasn't caused by the campaign feature's own code — it's exactly the kind of "unexpected state found while testing" that should be written down, not quietly fixed and forgotten.

## Phase 10 — Voucher Engine (complete, deployed live)

Net-new module — Phase 1's grep of `voucher|promo_code|discount_code` across `src/` found zero matches, so this is genuinely additive, not an improvement of something existing.

**Built (migrations `20260806001800`–`20260806002200`):**
- `vouchers` (scope: `platform`/`merchant`/`product`/`category`/`shipping`; discount type `percent`/`fixed`; min spend, usage limit, per-user usage limit, max-discount cap, active window) and `voucher_redemptions` tables.
- `resolve_voucher()` — the single source of truth for whether a code is usable right now for a given buyer/merchant/cart, called by both `quote_order()` (preview) and `place_order()` (authoritative, re-validated at the moment of purchase so a code that expired or hit its limit between quote and place is caught, not silently honored). `validate_voucher()` is a thin auth.uid()-bound wrapper for direct client calls.
- `quote_order()`/`place_order()` extended with an optional `p_voucher_code` param (default `null`, so every existing caller keeps working unchanged) — same "server recomputes/re-validates everything" pattern already used for campaign pricing.
- Admin `Vouchers.jsx` (`/admin/vouchers`, new `vouchers` staff permission) — create/edit/enable-disable/delete any scope, including platform-wide and shipping.
- Merchant `Vouchers.jsx` (`/merchant/vouchers`) — create/edit/enable-disable/delete vouchers scoped to their own store/products/shipping (no platform option).
- `Reseller/Checkout.jsx` — voucher-code input + Apply/Remove, a discount line in the price breakdown, and the code passed through to the actual order-placement call.

**Security gap found and fixed before shipping the UI (not caused by any UI code — found while building on top of the already-deployed schema from a prior session):** the first voucher migration (`20260806001800`) granted `insert, update, delete` on `vouchers` directly to `authenticated`, with RLS only checking `created_by = auth.uid() or is_admin()`. That check never looks at the caller's *role* or the voucher's *scope*, so any authenticated reseller or customer could have inserted a row with `scope='platform'` and `created_by=themselves` — a live, redeemable, marketplace-wide discount voucher created by anyone. Closed in `20260806002100_voucher_write_rpcs.sql` by revoking the direct grants and adding `create_voucher()`/`update_voucher()`/`delete_voucher()` — security-definer RPCs that validate role and force `merchant_id` to the caller's own id for any non-admin caller (the client-supplied value is ignored, not just checked). Scope/code/merchant/product/category are immutable after creation by design — edit price/window/limits or deactivate and create a new voucher if the target itself needs to change, the same tradeoff already made for `campaigns`.

**Second gap found while wiring the checkout UI:** `Checkout.jsx` never calls `place_order()` directly — it calls `place_receiver_shipping_order()`/`place_customer_receiver_shipping_order()` (fixed-arity wrappers from `shipping_engine_migration.sql`) that hardcoded the call into `place_order()` with no voucher parameter at all. Without fixing this, a voucher would have shown correctly in the `quote_order()` price preview and then been silently dropped at the real money-moving step — the exact "shopper sees a discount, gets charged full price" bug class Phase 1/9 already found and fixed once for campaigns. Fixed in `20260806002200_voucher_checkout_wrapper_functions.sql` by adding an optional `p_voucher_code` param to both wrappers (dropped and recreated, not overloaded, so every existing caller keeps working).

**Live-verified via direct SQL against the deployed database** (this project's established methodology for backend-only verification, per Phase 1/2's own precedent — see below for what this did *not* cover):
1. Simulated a real, non-admin, non-merchant reseller account attempting `create_voucher(..., scope='platform', ...)` → correctly rejected with `FORBIDDEN`.
2. As the admin/merchant demo account, created a real `scope='product'` voucher on a live product ("Barako Drip Coffee 10-Pack"). Called `quote_order()` as the reseller test account with the code applied → correct wholesale subtotal, correct 10%-off discount amount, correct total.
3. Called `quote_order()` with a nonexistent code → `voucher_valid: false`, `voucher_error: 'VOUCHER_NOT_FOUND'`, full undiscounted subtotal charged instead — confirms an invalid code fails closed rather than open.
4. Simulated the same non-owner reseller attempting `update_voucher()` on the admin's voucher → correctly rejected with `FORBIDDEN`.
5. Inserted a redemption row against the test voucher, then attempted `delete_voucher()` → correctly rejected with `VOUCHER_HAS_REDEMPTIONS` (delete is meant to be blocked once a voucher has real usage; disable it instead).
6. Cleaned up the test voucher and redemption row afterward.

**Not verified — full browser checkout end-to-end (voucher input → Apply → Pay & Place Order) was not exercised**, and this is a real gap, not just a formality: attempting it hit an existing, unrelated `DEVICE_APPROVAL_REQUIRED` block on the deployed site (a stale/other-device session already occupying the only real login available in this environment) — correctly blocked by that security trigger rather than bypassed, same as the identical situation Phase 9 already ran into and correctly declined to work around. No OTP-authenticated browser session was available this session to complete a real Reseller checkout. **Recommend the project owner run one real checkout with a voucher code applied before fully trusting this in production** — same recommendation Phase 9 left for campaign checkout, still open for both.

## Next step

All 13 phases from the original spec are now built and deployed. Nothing left on the checklist itself — the one open item is the real-browser-checkout verification called out above, which needs the project owner (a real OTP-authenticated session), not another automated pass.

## Phase checklist (from user's spec)

- [x] Phase 2 — Merchant Campaign Center (submission workflow, statuses; performance view deferred — needs Phase 9's checkout-campaign wiring to attribute orders to a campaign accurately, see note below)
- [x] Phase 3 — Product validation engine (core checks live in `submit_campaign_product()`: ownership/active/stock, schedule, pricing floor/ceiling, overlapping-campaign conflict, stock-allocation-vs-stock)
- [x] Phase 4 — Pricing rule engine (`requires_approval`/`min_discount_percent`/`max_discount_percent` on `campaigns`, configurable in Admin's Add Campaign form; Merchant submission panel shows live original/suggested/minimum-allowed price + validation status)
- [x] Phase 5 — Bulk import (CSV: sku/product_id, campaign_price or discount %, stock_allocation; per-row validation report; every row runs through the real `submit_campaign_product` RPC, no separate looser path)
- [x] Phase 6 — Bulk export (Merchant: per-campaign; Admin: all campaigns/merchants; reuses existing `utils/excel.js` `exportExcel`, no new library added)
- [x] Phase 7 — Automatic campaign enrollment (auto-approve when `requires_approval=false`, done in Phase 2; schedule-driven activation done in Phase 11's scheduler below)
- [x] Phase 8 — Automatic campaign benefits. `utils/campaigns.js` gained `getActiveCampaignProducts()` (per-product campaign data, was completely missing from every product-listing page before this — only the old whole-store `%` discount was ever fetched). Wired into Catalog.jsx (new "On campaign right now" featured strip — placed here, not Home.jsx, per `TASK7/PROJECT_ARCHITECTURE.md`'s Landing-is-marketing-only rule), ProductDetail.jsx (campaign name + live countdown via new `CampaignCountdown` component + original price struck through + campaign price), ResellerStorefront.jsx (campaign badge + struck-through price — previously fetched campaign data but never rendered it, always showed full price), MerchantStore.jsx (didn't call `applyCampaignDiscount` at all before this — campaign pricing never appeared on a merchant's own public store page; added a "Campaign Collection" featured strip too). Live-verified on the deployed site: badge "CAMPAIGN 30% OFF" on Catalog, and on ProductDetail the campaign name, "1d 23h left" countdown, ₱200 struck through → ₱140, and correct buying-price summary all rendered correctly against a real `campaign_products` row.
- [x] Phase 9 — Automatic Promotion Engine (`place_order()`/`quote_order()` now call `resolve_campaign_price()`, the exact fix Phase 1 flagged as critical; `order_items.campaign_id` added for future attribution. Core helper live-verified via direct SQL against a real active `campaign_products` row — correct price and campaign_id returned. Full browser checkout NOT exercised end-to-end: the demo account's product page has no buy UI for the admin role by design, and a raw-SQL simulated auth session was correctly blocked by an existing device-approval security trigger rather than bypassed. **Recommend a real Reseller-account checkout test before fully trusting this in production.**)
- [x] Phase 10 — Voucher Engine (net-new module; see write-up below)
- [x] Phase 11 — Campaign Scheduler (real `pg_cron` job `jom-hub-campaign-scheduler`, hourly at :20, following the exact guarded-schedule pattern of the existing `jom-hub-operational-maintenance` job; live-tested all 4 transitions: approved→active, →expired, active→paused, paused→active)
- [x] Phase 12 — Notifications (reuse existing `notifications` table from the integrations scaffolding; see write-up below)
- [x] Phase 13 — Admin Center upgrades (create/edit/duplicate/pause/resume/archive, approve/reject submissions, rule config, reports; see write-up below)

## Phase 12 — Notifications (complete, deployed live)

`public.notifications`/`create_notification()` (from `20260804000100_integration_scaffolding.sql`) had been deployed since Phase-8-era work but had zero producers — confirmed by grepping every migration for `create_notification(`, which only ever matched its own definition. Its own comment explains why: it's "for new [events with] no natural source row to derive a notification from," unlike the merchant/reseller/admin notification bells (`RoleNotifications.jsx`/`AdminNotifications.jsx`), which are pure derived feeds computed live from orders/withdrawals/wallets/etc. — no stored notification row involved at all, confirmed by grepping the whole frontend for `.from('notifications')` (zero matches before this phase).

**Two producers added** (migrations `20260807000200`–`20260807000300`), both from functions that already run as security definer and already have (or can cheaply get) a `merchant_id` to notify:
- `review_campaign_submission()` — notifies the submitting merchant on approve/reject, with the campaign/product name and (on reject) the admin's reason.
- `run_campaign_scheduler()` — rewritten from four bulk `UPDATE...FROM` statements (which only ever returned an aggregate row count) into four `FOR`-loops driven by `UPDATE...RETURNING` (a plain PL/pgSQL `FOR ... IN query` loop accepts any row-returning command, DML included), so each affected `campaign_products` row's `merchant_id` is available to notify for all 4 transitions: activated, expired, paused, resumed.
- `place_order()` — notifies a voucher's owning merchant (`vouchers.merchant_id`, when set — platform-wide vouchers have no single merchant to notify and are skipped) when their voucher gets redeemed.

**Frontend:** `RoleNotifications.jsx` (the shared merchant+reseller bell) now also reads unread `public.notifications` rows for the signed-in user alongside its existing derived-feed items, with a realtime subscription on the table so a new one appears without a refresh, and marks `read_at` (via the existing `notifications_owner_mark_read` RLS policy) when opened — on top of, not replacing, the existing localStorage "seen" tracking used by the synthetic items. `AdminNotifications.jsx` was left unchanged: both producers target a merchant's `owner_id`, so there's currently nothing admin-facing to read from this table (admins already see pending submissions directly via Admin Campaigns' own "Pending" panel).

**Live-tested via direct SQL against the deployed database:** seeded a temporary campaign + `campaign_products` row, ran `run_campaign_scheduler()` for real, confirmed the `activated` notification row (correct `owner_id`, title, body, link); reset the same row to `pending` and called `review_campaign_submission(..., true, null)`, confirmed the `approved` notification row. Cleaned up all test rows afterward. Frontend bell rendering itself (not just the backend write path) wasn't exercised in a browser — same open item noted for Phase 9/10 below.

**Incident found (again) while testing, fixed the same way as before:** mid-test, an unrelated insert on `campaigns` failed with `VALID_ADMIN_OWNER_REQUIRED` from the existing `guard_campaign_privileged_writes()` trigger. Checked and found the owner/demo account (`bc37187b-...`) once again stuck with `profiles.role = 'reseller'` instead of `'admin'` — the exact same "demo role switch left mid-way" incident already documented above for Phase 8/9, not caused by anything in this session. `previous_role` still correctly held `'admin'`, so it was the same safe, exact restoration (`role = previous_role, previous_role = null`). This is now the second time this has happened; if it recurs, the underlying "Back to Admin" completion path in the demo-switch UI itself may be worth a closer look rather than just restoring it again each time.

## Phase 13 — Admin Center upgrades (complete, deployed live)

Closed the concrete gaps in `Admin/Campaigns.jsx` against the phase's checklist — create and toggle (enable/disable) already existed from earlier phases; edit, duplicate, and archive did not:
- **Edit** — a campaign's name/description/discount/dates/submission rules can now be changed after creation (previously only the is_active toggle was possible; fixing a typo or adjusting dates meant no path but delete-and-recreate).
- **Duplicate** — clones a campaign as a new, disabled draft (`(Copy)` suffix) for a merchant running a similar campaign again, instead of re-typing every field.
- **Archive** — additive `campaigns.archived` boolean column (`20260807000100_campaign_archive_column.sql`); archived campaigns are hidden from the default admin view (a "Show archived" checkbox reveals them) without deleting any row or the `campaign_products`/order history hanging off it. Independent of `is_active` — a campaign can be archived and re-shown regardless of its live/disabled state.
- Approve/reject submissions and rule config were already done in Phases 2/4; not touched further here.

**Reports** — built the "campaign performance view" this file itself flagged back in Phase 2 as not-yet-possible, and Phase 9's own note said would become exact once `order_items.campaign_id` existed. It now does (since Phase 9), so it was built for real: `Admin/Reports/CampaignPerformanceReport.jsx` (`/admin/reports/campaigns`), following the same `ReportToolbar`/`SummaryCards`/date-range/Excel-export convention as every other report in this app. Attributes revenue, units sold, and an estimated discount-given figure to each campaign by reading `order_items.campaign_id` directly — exact per-order attribution, not the approximate date-range-overlap join this file originally ruled out as unreliable. ("Estimated" discount specifically: it compares against each product's *current* price as the regular-price baseline, since no historical price snapshot exists anywhere in this app's reports — same simplification every other report already makes, not a new gap introduced here.)

Verified the underlying query's join/column semantics directly against the live database via SQL — structurally correct, and confirmed there is currently no real order data with a stamped `campaign_id` yet (expected: Phase 9's campaign-checkout wiring has only ever been verified via direct SQL, never exercised through a real browser checkout — see the open item below), so the report correctly renders its empty state today rather than showing fabricated numbers.

## Open item carried across Phases 9, 10, 12, and 13: real browser checkout still unverified

Every phase that touches money at checkout (Phase 9's campaign pricing, Phase 10's vouchers, Phase 12's redemption notification, Phase 13's performance report reading real order data) has been verified by direct SQL against the live database, never by an actual signed-in browser session placing a real order. Two different sessions now have independently hit real obstacles trying: Phase 9 hit a device-approval security trigger when attempting a simulated auth session (correctly left alone rather than bypassed), and this session hit the same `DEVICE_APPROVAL_REQUIRED` block on the one real login available in this environment. **This is the single most important remaining recommendation across this entire file: have the project owner run one real Reseller checkout, with a voucher code applied, against a live campaign-priced product, before fully trusting Phases 9/10/12/13 in production.** Everything downstream of `place_order()`/`quote_order()` has been checked as thoroughly as direct SQL access allows, but "the SQL logic is correct" and "a real shopper's browser checkout works" are not the same claim, and only the second one is what actually matters commercially.
