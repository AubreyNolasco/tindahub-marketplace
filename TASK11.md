# Automatic Distance-Based Shipping Fee, PSGC+Leaflet-Only Address System — Progress & Handoff

**Status: built, migrated to production, AND now live click-tested end-to-end (this pass) — see "Live verification" below. Gap 1 (dispatch blocked for automatic orders) is fixed and confirmed working in production. Gap 2 (Lalamove vehicle-type selection) is built and its migration is pushed, but Gap 2 itself is still not live-tested (needs a real connected Lalamove account, which this codebase has never had). Several real bugs found during this pass were fixed and pushed — see "Bugs found and fixed this pass" below.**

## Live verification (this pass)

Test accounts `merchant@gmail.com`/`reseller@gmail.com` have been deleted from production. Use **Admin → Test Accounts → Admin demo mode** instead (`/admin/test-accounts`): "Switch to Reseller Mode" / "Switch to Merchant Mode" temporarily flips your own admin account's role so you can place real orders and manage real products against the "Admin Demo Merchant" storefront, then "Back to Admin" (shown at the top of every page while switched) returns you to admin. This is the supported way to test now — update any future doc/handoff that assumes the old Gmail test accounts still exist.

Ran a full real order end-to-end against production using this mode:
1. Created a customer with a PSGC address + Leaflet map pin (`AddressFields`/`LocationPickerMap` — confirmed the pin-drop, drag, and clear mechanics all work).
2. Checked out as Reseller against Admin Demo Merchant → **checkout showed "Delivery Fee (249.0 km, Sedan) ₱3,850.00", calculated live via `quote_order()`** — confirmed the automatic pricing pipeline built in this session's earlier passes works correctly in production, not just in migration-compiles-cleanly theory.
3. Paid & placed the order — wallet debited correctly, order created with `shipping_payment_method='prepaid_wallet'`.
4. Switched to Merchant Mode, marked the order Processing → **order list showed the "Paid — ready to dispatch" badge** (Gap 1's frontend fix), opened the order → **"Shipping fee (charged automatically): ₱3,850.00" banner + "Enter delivery details" button** (Gap 1's `DeliveryModal.jsx` fix) → filled in provider/tracking/proof → **"Save and mark Shipped" succeeded**, order status went to `Shipped`.

This is the exact click-through TASK11 previously flagged as "not done" for Gap 1 — it now works, confirmed live, not just via a clean `supabase db push`.

## Bugs found and fixed this pass

Found via a background `/code-review high` pass over the full branch diff, plus one found by hand while live-testing the map picker. All fixed, migrated, and re-verified (`npm run lint` clean, `npm test` 45/45, `supabase db push` clean).

- **`LocationPickerMap.jsx` stale marker after Clear** — reproduced live: clicking "Clear" on a pinned address set `latitude`/`longitude` to `null` and correctly updated the "No exact location pinned yet" badge, but the Leaflet marker glyph stayed visually parked at the old position, contradicting the badge. The marker-sync `useEffect` returned early on `null` coordinates without ever removing the existing marker. Fixed: it now calls `marker.remove()` and clears the ref when coordinates go null.
- **`quote_order()` missing the PH bounding-box check `place_order()` has** (`20260813000800_quote_order_bounding_box_fix.sql`) — a delivery pin placed outside the box (trivial via the un-geofenced Leaflet picker) would show a calculated fee at checkout *preview* time, then silently fall back to manual/pay-on-delivery when `place_order()` ran moments later for the same coordinates — the previewed amount never matched what was actually charged. Same migration also widens the box from `lat between 4 and 21` to `4 and 21.5` on both functions, since the strict 21 cutoff excluded the northernmost inhabited Philippine territory (Y'Ami/Itbayat, Batanes, up to ~21.2°N) — a legitimate domestic address could otherwise be quoted then rejected.
- **Admin Shipping Pricing preview: `0` km treated as "fill in the fields"** (`ShippingPricing.jsx`) — the guard used `!distance`, so a deliberately-typed `0` (a valid, allowed input per the field's own `min="0"`) showed the same message as an empty field. Fixed to check for the empty string explicitly.
- **Admin Shipping Pricing preview: multiplier default mismatch** (`ShippingPricing.jsx`) — the live preview defaulted `road_directness_multiplier` to `1` when the field was transiently empty, while `save()` defaults it to `1.3` — the previewed fee could understate what would actually be saved. Aligned both to `1.3`.
- **`resolveBarangayCode`/`resolvePsgcCodes` ILIKE wildcard injection** (`src/lib/services/psgc.js`) — free-text province/city/barangay names saved before PSGC codes existed are matched with `.ilike()` without escaping `%`/`_`, so a stray wildcard character in old saved text could match an unrelated row and attach the wrong code. Fixed with a `%`/`_`/`\` escaper applied before every `.ilike()` call.

**Reviewed but not changed** (real findings, lower priority, left for a future pass — full detail in the code-review agent's report if picked up again): `ShippingFeeModal.jsx` lost its specific error-code-to-message mapping (`MISSING_PACKAGE_INFORMATION` etc.) when the old "Free distance estimate" button was removed, so a merchant whose products are missing weight/dimensions has no in-app way to learn why automatic pricing keeps failing for them — needs a small design decision on where that diagnostic should surface, not just a one-line fix; the `shipping_payment_method === 'prepaid_wallet'` dispatch-ready check is independently re-typed as a raw string comparison in four places (`DeliveryModal.jsx`, `PurchaseHistory.jsx`, `Merchant/Orders.jsx` ×2) instead of one shared predicate; `calculate_standard_shipping()` runs its own per-item product-row loop and is called twice per checkout (once from `quote_order()`, once from `place_order()`), which is correct but does more DB round-trips than necessary on the checkout hot path.

Full architectural audit + rationale (why each decision was made, what was rejected and why) is a published artifact the owner already has: ask them for the link if it's not in your context, or re-derive from this file — this doc is the code-accurate summary, that one is the reasoning.

## Original request

Owner wanted the address/map/shipping/checkout system redesigned so PSGC (Philippine Standard Geographic Code) is the authoritative address hierarchy, a map lets the customer confirm an exact delivery pin, and shipping fee is calculated automatically and shown *before* the order is placed — inspired by Lalamove's distance-based pricing but not copying it, admin-configurable, with the backend as the sole source of truth (never trust a client-sent fee).

Two scope-defining decisions the owner gave mid-session, both **final, do not re-litigate without asking**:
1. **No merchant-proposed shipping fee as the default path.** Automatic, distance-based, shown to both reseller and merchant — the old "merchant proposes a fee after the order exists, reseller accepts/declines" flow is a *fallback only*, not the normal case.
2. **No external APIs at all — PSGC + Leaflet only.** No LocationIQ, no Nominatim, no OSRM, no Google Maps/Routes. Distance is straight-line (haversine) between two map pins × a configurable multiplier, not real road distance. This was a hard reversal of an earlier draft that recommended self-hosting OSRM — the owner explicitly rejected adding any new hosted service or API key.

## What was built

### Database (5 new tracked migrations, `supabase/migrations/`, all applied to the live project `ttscpfsodrcyllyvvqzb`)

- **`20260813000100_shipping_engine_tracked_migration.sql`** — moved the still-live parts of `supabase/shipping_engine_migration.sql` (an *untracked* file outside the numbered migration sequence — the same pattern that broke `quote_order()` once in production before, per `20260723000900_quote_order_self_contained_pricing.sql`'s own comment) into a real tracked migration: `shipping_settings` table, `calculate_standard_shipping()`, the `require_complete_customer_address`/`save_signup_address` triggers, and the columns they all depend on. Dropped four confirmed-dead RPCs (`place_standard_order`, `place_customer_standard_order`, `place_mapped_order`, `place_customer_mapped_order` — nothing in `src/` ever called them, only `place_receiver_shipping_order`/`place_customer_receiver_shipping_order` and `calculate_standard_shipping` directly are used) and the unused `shipping_distance_quotes` table. `supabase/shipping_engine_migration.sql` and `supabase/test_account_auth_setup.sql` got a "SUPERSEDED — do not run this file again" header rather than being deleted, to preserve history.
- **`20260813000200_shipping_pricing_engine.sql`** — `haversine_km(lat1,lng1,lat2,lng2)` (pure SQL, `immutable`, no external call). `shipping_pricing_rules` table: versioned/effective-dated config mirroring the existing `system_policies` precedent (draft → published → archived, one published row per `vehicle_type` enforced by a partial unique index, transitioned through `publish_shipping_pricing_rule(target_id)` not a plain UPDATE). Seeded with today's exact rate structure as v1/published for both `motorcycle` (₱49 base, 5km included, ₱6/km, ₱5/km beyond) and `sedan` (₱100 base, ₱18/km, ₱15/km beyond) — both `rounding_increment=5`, `road_directness_multiplier=1.3`. Rewrote `calculate_standard_shipping()` to read these instead of hardcoded literals; vehicle-selection logic (weight/dimension thresholds, special-handling detection) is unchanged. The function's return jsonb now includes `'rule_id'` so callers don't need a second lookup.
- **`20260813000300_remove_locationiq_integration.sql`** — deletes the now-dead `integration_configs` row for `maps.locationiq`.
- **`20260813000400_automatic_shipping_checkout.sql`** — the core of this work. `orders` gets `shipping_pricing_rule_id` and `shipping_fee_breakdown jsonb`. `quote_order()` and `place_order()` both **dropped their `p_shipping_fee` parameter entirely** — they now take `p_delivery_latitude`/`p_delivery_longitude` instead, which carry no financial meaning by themselves. Inside `place_order()`: looks up the merchant's `pickup_latitude`/`pickup_longitude`, and if both pins exist (and the delivery coordinates pass a rough PH bounding-box check, lat 4–21 / lng 116–127), computes `haversine_km(...)` then calls `calculate_standard_shipping()` inside a `begin/exception when others` block. **On success**: `shipping_fee` = the real computed fee, `shipping_payment_method = 'prepaid_wallet'`, `shipping_payment_status = 'paid'`, and it's folded into the same wallet debit as subtotal + processing fee, in the same transaction. **On any failure** (no pin, `MISSING_PACKAGE_INFORMATION`, `MANUAL_QUOTATION_REQUIRED`, anything) — falls back to exactly today's pre-existing behavior: `shipping_fee = 0`, `shipping_payment_method = 'receiver_pays_on_delivery'`, and `propose_order_shipping_fee`/`respond_order_shipping_fee` (untouched by this migration, still in `20260722000900_shipping_fee_confirmation.sql`) remain available as the manual override path. Checkout never hard-fails just because automatic pricing wasn't possible for a given cart. `place_receiver_shipping_order()` no longer forces `shipping_fee back to 0` after calling `place_order()` (it used to, unconditionally — that line is gone). `place_customer_receiver_shipping_order()` (order-for-a-saved-customer path) pulls the customer's own saved `latitude`/`longitude` server-side rather than trusting anything from the client, consistent with how it already re-validates the customer's address.
- **`20260813000500_barangay_codes.sql`** — `barangay_code text references psgc_barangays(code)` added to `profiles`, `merchant_profiles`, `customers`. Province/city already had code columns (`psgc_provinces`/`psgc_cities`); barangay was name-only before this.

### Frontend

- **`src/components/address/AddressFields.jsx`** — fully rewritten. Removed the LocationIQ search box, the debounced auto-geocode-from-typed-address effect, and the reverse-geocode-on-pin-drop effect entirely. Address entry is now: PSGC cascading dropdowns (Province → City → Barangay, unchanged) + manual street/house-number text + a Leaflet map pin the customer places themselves (`LocationPickerMap.jsx`, untouched — it was already pure Leaflet/OSM-tiles with no geocoding dependency). `selectBarangay` now also stores `option.code` as `barangayCode` in the value shape.
- **`src/lib/services/psgc.js`** — removed `resolveGeocodedPsgc()` and its private helpers (`normalizePlace`, `findPlace`, `listAllCities`) — that whole block existed only to snap a geocoder's guess onto a real PSGC row, and there's no geocoder left to guess anything. Added `resolveBarangayCode(cityCode, barangayName)`, mirroring the existing `resolvePsgcCodes()` pattern, for backfilling `barangayCode` on records saved before that column existed.
- **Deleted**: `src/lib/locationiq.js`, `src/lib/openstreetmap.js`, `src/lib/services/maps.js` (the `isMapsEnabled()` LocationIQ on/off gate — nothing left to gate), `src/components/order/RouteMap.jsx` (confirmed orphaned once `ShippingFeeModal.jsx`'s OSRM-based "Free distance estimate" button was removed — nothing imports it anymore), `src/utils/shipping.js` (a dead JS mirror of the old hardcoded formula, confirmed unused by any import before deleting).
- **`src/pages/Reseller/Checkout.jsx`** — the main checkout page. `AddressFields` now mounted with `withCoordinates`. New `deliveryLatitude`/`deliveryLongitude` derived value (customer's saved coords when ordering for a saved customer, else the live `addressParts.latitude/longitude`) — fetched via an extended `loadCustomerAddress()` that now also selects `latitude, longitude` from `customers`. The `quote_order` RPC call and its `useEffect` dependency array both updated to pass/react to these coordinates instead of a hardcoded `p_shipping_fee: 0`. New `shippingCalculated`/`shippingFee` derived from `quote.shipping_status === 'calculated'`. Order Summary now shows a real "Delivery Fee (X.X km, Vehicle)" line with the actual peso amount when calculated, or "Confirmed after ordering" when not. The static "shipping is paid on delivery" card is now conditional — a teal "included in your payment" card when automatic, the original mango warning card (unchanged copy) when falling back to manual. The pre-submit "Important shipping reminder" modal now only opens when `!shippingCalculated` (`handleSubmit`'s branch condition changed). `placeOrder()` passes `p_delivery_latitude`/`p_delivery_longitude` to `place_receiver_shipping_order` (self-checkout only — the customer-order RPC doesn't need them, server derives them).
- **`src/components/order/ShippingFeeModal.jsx`** — removed the "Free distance estimate" button and its `getFreeEstimate()` handler entirely (it called the now-deleted `route-estimate` edge function, and would have double-applied the new `road_directness_multiplier` if left calling `calculate_standard_shipping` directly with an already-road-distance number). "Get delivery quote" (live Lalamove) and manual fee entry remain — this modal is now reached only for the `MANUAL_QUOTATION_REQUIRED`-style fallback case, per the owner's decision #1 above, though **the actual gating of when this modal is reachable was not changed in this pass** — see Gap 1 below.
- **`src/pages/Admin/ShippingPricing.jsx`** (new) — Admin page at `/admin/shipping-pricing` ("Shipping & Order Fees" in the sidebar, Platform Config section, `Milestone` icon). Two parts on one page: (1) a `DataTable` over `shipping_pricing_rules` with Add/Edit/Publish/Archive, mirroring `LegalSettings.jsx`'s exact draft→publish→archive UX including a `ConfirmDialog` before publish/archive; the edit form has a live fee preview (distance input → computed fee) using a new pure JS helper, not the SQL function, so it works without a round-trip while editing a draft. (2) A plain form over the `revenue_settings` singleton row (processing fee %, min/max, tax reserve) — direct `update`, mirroring `HomepageEditor.jsx`'s persistence style. Wired into `App.jsx` (new lazy route) and `AdminLayout.jsx` (new sidebar item).
- **`src/utils/geo.js`** (new) — `haversineKm(lat1,lng1,lat2,lng2)`, a JS mirror of the SQL `haversine_km()`. Deliberately kept as a duplicate (unlike the old pricing-formula mirror that was removed) because haversine is universal, unchanging math, not a business rule that can drift.
- **`src/utils/shippingPreview.js`** (new) — `previewShippingFee(rule, distanceKm)`, used only by the admin form's live preview described above. Explicitly NOT the vehicle-selection logic (that needs a real cart) — just the fee-from-rule arithmetic, for sanity-checking a draft rate before publishing.
- **`src/utils/address.js`** — `emptyAddressParts()` now includes `barangayCode: null`.
- **`src/pages/ProfileAddress.jsx`, `src/pages/Auth/Onboarding.jsx`, `src/pages/Reseller/Customers.jsx`, `src/pages/Reseller/StorefrontOrderRequests.jsx`** — all four places that write structured address columns now also write `barangay_code`. `ProfileAddress.jsx` additionally resolves a saved barangay name back to its code on load (via the new `resolveBarangayCode`), the same way it already does for province/city.
- **`vercel.json`** — removed the now-unnecessary `api.locationiq.com` CSP `connect-src` entry. Fixed `Permissions-Policy: geolocation=()` → `geolocation=(self)` — the old value fully blocked the browser Geolocation API including for the app's own origin, which would have silently broken `AddressFields.jsx`'s pre-existing "Use my current location" button (this bug predates this session; fixed here because the pin is now load-bearing with no autocomplete fallback).
- **`.env.example`** — removed `VITE_LOCATIONIQ_API_KEY`.

### Data fix

- **`supabase/grant_test_accounts_full_access.sql`** — the one-time, re-runnable script that seeds the `merchant@gmail.com`/`reseller@gmail.com` test accounts had `business_address = 'Internal test address - not for real deliveries'` with no coordinates, which would break distance calculation for anyone testing against it. Fixed to a real structured address (Barangay Bagong Pag-asa, Quezon City, Metro Manila, `pickup_latitude/longitude` 14.648/121.035). **This script was NOT re-run against the live database this session** — it's a manual SQL Editor script, not part of the migrations `supabase db push` applies. If the owner wants the live test account's address actually updated, that script needs to be run once, by hand or on request.

### Tests (all passing, 45/45 total via `npm test`)

- `tests/geo.test.js` — `haversineKm` against known coordinate pairs (identity, symmetry, a real ~2km Metro Manila pair, Manila-to-Cebu long distance).
- `tests/shippingPreview.test.js` — `previewShippingFee` against the seeded motorcycle rate: base-fee-only, additional-km charge (verified against the brief's own 7.8km worked example: ₱95), rounding-increment behavior, exact included-distance boundary, min/max clamping, surcharge, road-directness-multiplier scaling.
- Deleted `tests/addressMap.test.js` (tested the now-deleted `openstreetmap.js`) and `tests/shipping.test.js` (tested the now-deleted `utils/shipping.js`).

## Verification done

- `npm run lint` clean, `npm test` 45/45 passing — checked repeatedly through the session, not just at the end.
- `supabase db push` applied all 5 migrations cleanly (a broken function body would have failed the push outright — a clean push is itself a real SQL compile check).
- Live RPC spot-checks against the production project (via `@supabase/supabase-js` with the public anon key, no auth needed since these are public-read):
  - `shipping_pricing_rules` select confirmed both motorcycle/sedan v1 rows exist with `status: "published"` and the exact seeded numbers.
  - `haversine_km` RPC call (14.6480,121.0350 → 14.6537,121.0685) returned `3.659...` km — correct order of magnitude for two real nearby QC coordinates.
- **Not done**: no browser click-through of the new checkout flow, the admin Shipping Pricing page, or the address form. `git commit` + `git push` to `agent/fix-address-map-sync` were done; no PR opened.

## Known gaps — read before continuing

### Gap 1 — FIXED: dispatch was blocked for automatically-priced orders

`set_order_delivery()` (was `20260723001900_fix_database_lint_warnings.sql:5-49`) — the RPC `DeliveryModal.jsx` calls to mark an order `shipped` and record courier/tracking info — required `shipping_fee_confirmation_status='accepted' and proposed_shipping_fee is not null`, a pair **only ever set by the manual negotiation flow**. The automatic path (`shipping_payment_method = 'prepaid_wallet'`) never touched those columns, so they stayed `null` — a merchant had no way to mark an automatically-priced order as shipped through any existing UI. Same condition also gated the `enforce_shipping_fee_before_dispatch()` trigger directly on the `orders.status` transition.

**Fixed in `20260813000600_prepaid_dispatch_fix.sql`** (applied to production): both the trigger and `set_order_delivery()`'s `where` clause now treat `shipping_payment_method = 'prepaid_wallet'` as an alternative already-confirmed state — an order is dispatch-ready if *either* the automatic fee was charged *or* the manual negotiation was accepted. `actual_shipping_fee` and the `p_actual_fee` compatibility check now use `coalesce(proposed_shipping_fee, shipping_fee)` so they resolve correctly on the prepaid path too (`proposed_shipping_fee` stays null there; `shipping_fee` holds the real charged amount).

**Frontend also fixed** (was silently broken in the same way — worth knowing even though the RPC fix alone would have surfaced a clear error instead of nothing):
- `DeliveryModal.jsx` — `submit`'s guard, the header copy, and the fee shown/sent to the RPC all branch on a new `isPrepaid = order.shipping_payment_method === 'prepaid_wallet'` / `confirmedFee = order.proposed_shipping_fee ?? order.shipping_fee`, instead of assuming `proposed_shipping_fee` is always populated.
- `Merchant/Orders.jsx` — the order-detail "Shipping fee / dispatch" panel had exactly one branch, `!selectedOrder.shipping_fee_confirmation_status`, which is `true` for both "genuinely needs manual pricing" *and* "already prepaid automatically" (both leave that column null) — it was showing "enter the actual shipping fee for Reseller approval" on already-paid orders, with no way to reach "Enter delivery details" at all. Added a `shipping_payment_method === 'prepaid_wallet'` branch ahead of it showing the charged fee and the dispatch button directly. The list-view "Shipping fee" column badge got the same branch (new "Paid — ready to dispatch" badge) so it's visible without opening the detail view.
- `PurchaseHistory.jsx` (reseller side) — was already safe by construction (its branches only fire on non-null `shipping_fee_confirmation_status` values, so a prepaid order correctly fell through to `—`), but added a matching "Paid" badge for consistency/visibility rather than leaving it blank.

**Not done**: no live click-through — there's no real order in the `processing` status with `shipping_payment_method = 'prepaid_wallet'` yet in production (no merchant has pickup coordinates set except the unapplied test-account fix, so no real order has taken the automatic path yet). Verified via `npm run lint` (clean), `npm test` (45/45), and a clean `supabase db push` (a broken function body would have failed it). Whoever picks this up next should place one real test order end-to-end (merchant with a pickup pin + customer with a delivery pin) and click through Orders → dispatch to confirm live, not just trust the migration compiled.

### Gap 2 — BUILT (not yet migrated to production, not live-tested): vehicle-type choice for the actual courier booking

Built in this session, `20260813000700_lalamove_service_type.sql` + code changes below. **Not yet applied to the live database** (`supabase db push` not run this pass) and Lalamove itself is still marked "NOT LIVE-TESTED" in `lalamove.ts`'s own header — this only wires the plumbing, it doesn't newly validate against a real Lalamove account.

- `orders.delivery_service_type text` — new column. Set once, at `propose_order_shipping_fee` time (merchant picks a vehicle before getting an automatic quote), read back by `delivery-book` when it re-quotes/books after the Reseller accepts, so the booked vehicle matches what was quoted rather than the adapter's `MOTORCYCLE` default.
- `propose_order_shipping_fee` — new 6th param `p_delivery_service_type text default null`, validated server-side against `('MOTORCYCLE','CAR')` (defense in depth — the edge function already validates, but the RPC is reachable directly by any authenticated merchant).
- `_shared/delivery/types.ts` — `QuoteInput`/`BookingInput` both get an optional `serviceType?: string`, adapter-agnostic (an adapter that doesn't support vehicle selection just ignores it).
- `_shared/delivery/adapters/lalamove.ts` — `getQuote`/`createBooking` both pass `input.serviceType` through to `requestQuotation`.
- `delivery-quote/index.ts` — accepts `service_type` in the request body, validated against an `ALLOWED_SERVICE_TYPES` allowlist (`MOTORCYCLE`/`CAR` only — see VERIFY comment, the real Lalamove PH enum is still unconfirmed), passed into `getQuote`, echoed back in the response.
- `delivery-book/index.ts` — now selects `delivery_service_type` off the order and passes it into `createBooking`.
- `ShippingFeeModal.jsx` — new "Vehicle needed" dropdown (Motorcycle/Car) above "Get delivery quote"; selecting a vehicle re-quotes; the choice is only persisted (`p_delivery_service_type`) when the automatic-quote path was actually used (`accountId` is set) — a manually-typed fee sends `null`, same pattern as `quotationId`/`accountId`.
- Customer-facing charge is unaffected — the automatic platform estimate (Gap 1's motorcycle/sedan tiers) stays fixed at order time regardless of what vehicle the merchant later books, matching the owner's discussed recommendation (same as Shopee/Lazada shipping estimates never being re-billed).

**Not done**: migration not pushed to production; no live click-through (would also require a real connected Lalamove account, which nothing in this codebase has ever had — see the "NOT LIVE-TESTED" header in `lalamove.ts`); van/truck vehicle tiers were deliberately left out — the owner's own example was specifically "kotse" (car) vs. motor, so only those two were built rather than guessing at additional untested Lalamove enum values.

### Superseded — original Gap 2 investigation notes, kept for context

The owner asked specifically about this (see chat: "paano kapag kotse ang gusto book ni merchant tapos motor lang ang nasa system... iba-iba ang shipping fee ng courier type ni Lalamove"). Investigated and confirmed:

- `supabase/functions/_shared/lalamove.ts:86-111` (`requestQuotation`) accepts an optional `serviceType` param but defaults to `'MOTORCYCLE'` (`:102`) if not given.
- `supabase/functions/_shared/delivery/adapters/lalamove.ts` (`getQuote`/`createBooking`) **never passes a `serviceType`** — every Lalamove quote/booking this system has ever requested, or will request, asks for a motorcycle, regardless of actual order size. This predates this session but is directly relevant to it.

There are three genuinely separate "vehicle" concepts in the system now, and only one is wired end-to-end:
1. **The automatic platform estimate** (Motorcycle/Sedan tier, this session's work) — determines what the *customer* is charged, fixed at order time, never revisited.
2. **The real courier vehicle the merchant needs** — a merchant decision, made when actually arranging delivery, that can legitimately differ from #1 (e.g. cart data undersells real bulk, or the merchant just prefers a bigger vehicle).
3. **Lalamove's own `serviceType`** — currently hardcoded to MOTORCYCLE with no way to request CAR/VAN/etc.

**Discussed recommendation** (also not yet built): keep #1 fixed as what the customer pays — no re-billing the customer if the merchant's real courier costs more, same as how Shopee/Lazada shipping estimates work. Add a vehicle-type selector to `DeliveryModal.jsx`/the "Get delivery quote" flow in `ShippingFeeModal.jsx` so the merchant can request a real Lalamove `serviceType` matching what they're actually shipping, threading that through the adapter and edge functions (`delivery-quote`, `delivery-book`) which currently have no `serviceType` parameter anywhere in their request bodies either. The cost difference is the merchant's own fulfillment cost, not something the customer sees again.

### Smaller, lower-priority items noticed but not addressed

- No existing merchant has `pickup_latitude`/`pickup_longitude` set except the (unapplied, see above) test account fix — every real merchant needs to visit `ProfileAddress.jsx` (merchant mode) and drop a pin before their orders can price automatically at all. Until then, every one of their orders correctly (not buggy) falls back to manual/pay-on-delivery. Worth a heads-up notification or onboarding nudge to existing merchants, not built.
- `barangay_code` is only populated going forward (new saves) or lazily resolved on load for `profiles`/`merchant_profiles` (`ProfileAddress.jsx`). `customers` rows created before this session, and any address never re-opened in `ProfileAddress.jsx`, will have `barangay_code = null` indefinitely unless something explicitly backfills them. Not a functional blocker (barangay name-matching still works everywhere it did before), just incomplete data.
- `shipping_settings.buffer_percent` — an old, already-unused stub column — was left alone (not dropped) per the project's existing convention of not dropping columns speculatively. Confirmed nothing reads it, before or after this session.
